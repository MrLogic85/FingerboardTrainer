<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sleepyduck.fingerboardtrainer"
    android:versionCode="7"
    android:versionName="2.0.0.1" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.sleepyduck.fingerboardtrainer.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.sleepyduck.fingerboardtrainer.HistoryActivity"
            android:label="@string/title_activity_history" >
        </activity>

        <service android:name="com.sleepyduck.fingerboardtrainer.TimerService" >
        </service>
    </application>

</manifest>